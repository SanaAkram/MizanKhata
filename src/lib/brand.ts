/** App identity used on shared/printed artefacts (bills, etc). */
export const APP_NAME = "MizanKhata";

/** Public site — update once the domain is live. */
export const APP_URL = "mizankhata.app";

/** The little credit line every bill carries, premium or not. */
export const BILL_CREDIT = `Made with ${APP_NAME} · ${APP_URL}`;
